apiVersion: v1
kind: Service
metadata:
  name: product-service-node
  labels:
    app: product-service-node
spec:
  type: NodePort
  selector:
    app: product-service-node
  ports:
    - protocol: TCP
      port: 3006
      targetPort: 3006
